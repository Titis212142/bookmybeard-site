PORT=4000
FRONTEND_ORIGIN=http://localhost:5500,http://localhost:5173,http://localhost:3000
DB_PATH=./data/bookings.db

# Email: prefer Brevo API on Railway (HTTPS, no SMTP timeout).
BREVO_API_KEY=

# SMTP settings (fallback for local dev). If omitted, emails are logged only.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM=matis.monnin@gmail.com
CONTACT_RECIPIENT=matis.monnin@gmail.com
ADMIN_PASSWORD=change-me-admin-password
